export type AnalyzeOptions = {
    dir: string;
    reactProductionProfiling?: boolean;
    noMangling?: boolean;
    appDirOnly?: boolean;
    serve?: boolean;
    port?: number;
};
export default function analyze({ dir, reactProductionProfiling, noMangling, appDirOnly, serve, port, }: AnalyzeOptions): Promise<void>;
